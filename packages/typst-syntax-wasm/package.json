{
	"name": "texpile-typst-syntax-wasm",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"description": "Typst's official parser as a Lezer parser for CodeMirror",
	"license": "Apache-2.0",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts"
	},
	"scripts": {
		"_comment:build:wasm": "deliberately NOT named `build`: the root `pnpm build` runs `-r run build`, and pkg/ is committed precisely so that a checkout needs no Rust toolchain. Only someone changing lib.rs or the typst-syntax version runs this.",
		"build:wasm": "wasm-pack build --release --no-pack --target=bundler --out-dir=pkg && node scripts/postbuild.mjs",
		"test": "vitest run"
	},
	"dependencies": {
		"@lezer/common": "^1.2.3",
		"@lezer/highlight": "^1.2.1"
	},
	"devDependencies": {
		"vite-plugin-wasm": "^3.6.0",
		"vitest": "^1.6.1"
	}
}
