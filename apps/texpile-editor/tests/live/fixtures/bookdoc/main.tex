\documentclass{book}
\begin{document}
\chapter{Opening}

Book prose paragraph under the first chapter heading with enough words that the
line breaker wraps it across a couple of lines in the page body.

Second book paragraph so the fixture has more than one editable block here.

The display below gives the page a stretchable display skip, so the vertical
glue on this page can absorb small height changes the way pages of a real
mathematics book absorb them during justification of the full page body.
\begin{equation}
  e = m c^2 + \frac{a}{b}
\end{equation}

Filler alpha carries steady prose across several lines so the first page of
this fixture actually fills with body text and the page builder has genuine
decisions to make about where the flow of paragraphs must finally break off.

Filler bravo continues the same steady march of sentences down the page with
enough words in every line that the paragraph wraps four times before the
next block takes over and pushes the running total of lines further along.

Growth target paragraph sits mid page with steady prose that wraps across
several lines so an appended clause pushes exactly one further line into the
flow below it and the certificate machinery has real work left to certify.

Filler charlie keeps the column moving toward the bottom of the first page
with the same density of words as its neighbours so the eventual page break
lands somewhere inside the filler blocks that follow after this paragraph.

Filler delta adds yet another stretch of plain body text whose only purpose
is occupying vertical room on the page while remaining perfectly ordinary
prose that the warm engine can reproduce line for line without surprises.

Filler foxtrot lives on the second page and gives that page its own body of
editable text so cross page behaviour keeps a home in this fixture as well,
wrapping over enough lines to stay interesting for the paragraph matcher.

Closing bookdoc paragraph anchors the very end of the fixture with steady
words that no scenario currently edits so the document tail stays stable.

\end{document}
