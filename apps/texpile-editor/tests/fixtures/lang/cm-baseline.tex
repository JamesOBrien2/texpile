% No fontspec at all: LuaLaTeX's own default (Latin Modern, loaded by file name, so the font
% name it reports carries no quotes). The control -- this path was always fine and its records
% must stay byte-for-byte unchanged.
\documentclass{article}
\begin{document}
A plain Latin line.
\end{document}
